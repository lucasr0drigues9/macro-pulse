import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "World Order — The Race to Automate | Macro World View",
  description: "The race to automate will decide who leads the next world order. US (Terafab, CHIPS Act), China (#1 robot installer, rare earth control), Europe (ASML monopoly). How geopolitical competition accelerates the AI & Robotics Race supply chain.",
};

export default function WorldOrderLayout({ children }: { children: React.ReactNode }) {
  return children;
}
