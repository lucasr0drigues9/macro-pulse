import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "The AI Race — Full Investment Thesis | Macro World View",
  description: "The complete AI Race supply chain thesis. Supply chain map, regime-aware entry strategy, Druckenmiller-style forward catalysts, and the historical parallels that prove it. Growth vs materials ETFs with live data.",
};

export default function AIRaceLayout({ children }: { children: React.ReactNode }) {
  return children;
}
