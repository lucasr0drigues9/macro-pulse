import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "China Tracker — How China Affects the AI & Robotics Race | Macro World View",
  description: "China controls 60% of rare earth mining, 90% of processing, and installs more robots than the rest of the world combined. Track China's real economy with proxy indicators and understand the impact on AI & Robotics Race supply chain ETFs.",
};

export default function ChinaLayout({ children }: { children: React.ReactNode }) {
  return children;
}
