import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "The AI & Robotics Race — Supply Chain Investment Thesis | Macro World View",
  description: "Terafab, robotics, and the materials supercycle. Track the supply chain ETFs (SMH, BOTZ, COPX, LIT, REMX, ARKQ, AIQ) that benefit from the AI and robotics industrial revolution. Regime-aware entry timing.",
};

export default function TerafabLayout({ children }: { children: React.ReactNode }) {
  return children;
}
